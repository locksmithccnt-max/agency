// Prisma removed — booking system uses Supabase directly via lib/supabase.ts
// This stub prevents import errors in legacy files that were not cleaned up.

export const prisma = {} as Record<string, unknown>;
export function getPrismaClient() { return prisma; }
